{
  "name": "your-project-name",
  "version": "1.0.0",
  "scripts": {
    "docker:up": "docker-compose -f docker-compose.dev.yml up -d",
    "docker:down": "docker-compose -f docker-compose.dev.yml down",
    "docker:clear": "docker-compose -f docker-compose.dev.yml down --rmi all"
  }
}
